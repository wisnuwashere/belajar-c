/*

  else dikaitkan dengan a>b

  before:

  if(n > 0)
    if(a > b)
        z = a;
    else
        z = b;
*/

// after:

#include <stdio.h>

int main()
{

  int a, b, z, n;

  if (n > 0)
  {
    if (a > b)
    {
      z = a;
    }
    else
    {
      z = b;
    }
  }

  return 0;
}

// masing-masing terdapat tutup kurung;